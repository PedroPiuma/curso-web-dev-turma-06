function checkSignal(number) {
    if (number > 0) {
        console.log(number + ' é positivo.')
    } else if (number < 0) {
        console.log(number + ' é negativo.')
    }
}

checkSignal(1)
checkSignal(2)
checkSignal(-1)
checkSignal(-6)